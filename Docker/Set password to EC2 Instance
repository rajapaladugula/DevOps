1. Open sshd_config file & set: PasswordAuthentication yes
    sudo vi /etc/ssh/sshd_config
2. Set password to user using passwd:
    sudo passwd <ec2-user>
3. Restart sshd service:
    sudo service sshd restart
